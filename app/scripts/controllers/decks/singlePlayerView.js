'use strict';

/**
 * @ngdoc function
 * @name WebFiles3App.controller:SinglePlayerView
 * @description
 * # SinglePlayerView
 * Controller of the WebFiles3App
 */
angular.module('WebFiles3App')
  .controller('SinglePlayerView', function ($scope) {
	
});