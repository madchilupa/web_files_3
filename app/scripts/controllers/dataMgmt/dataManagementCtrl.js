'use strict';

/**
 * @ngdoc function
 * @name WebFiles3App.controller:DataManagementCtrl
 * @description
 * # DataManagementCtrl
 * Controller of the WebFiles3App
 */
angular.module('WebFiles3App')
  .controller('DataManagementCtrl', function ($scope, $routeParams, $http) {
	$scope.displayData = {};
	
	$scope.displayData.gridDefinitionGridProps = {
	};
 });